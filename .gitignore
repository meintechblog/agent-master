# Runtime state
data/*.log
data/spawn.log
data/spawned-windows.json
data/soft-stop-state.json
data/briefed-peers.json
data/.influx-token
data/sources.json
data/health-state.json
data/update-state.json
data/registry-fill.json

# Local user registry — derived from data/registry.example.json on first install.
# Each user maintains their own.
data/registry.json

# External-LLM backend configs hold API keys — never commit.
data/external-llm.json

# Local artifacts
node_modules/
.DS_Store
*.bak
*.bak.*

# Local plist patches (installer regenerates per machine)
launchd/*.local.plist
.planning/

# Playwright / browser-driven testing artifacts (transient)
.playwright-mcp/
*.png
.backups/
*.jpeg
data/.wa-phone
data/github-tracker.json
data/agent-rules.json
data/test-cases.json
